import { Component } from '@angular/core';

@Component({
  selector: 'nav', //used to be "my-app"
  templateUrl: './nav.html', //already knows its in the /nav subfolder
})
export class navComponent  { 

 }
